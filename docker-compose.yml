services:
  haven-va:
    build: .
    restart: unless-stopped
    env_file: /volume1/docker/haven-va/data/.env
    volumes:
      - /volume1/docker/haven-va/data:/data
    environment:
      DATA_DIR: /data
      TZ: Europe/Lisbon
